# ============================================================
#  bot-sort-version  —  Python dependencies
# ============================================================
# PyTorch is a CUDA build on the dev machine (2.7.1+cu118). Install torch FIRST
# from the matching wheel index, then `pip install -r requirements1.txt`:
#
#   GPU (CUDA 11.8):
#     pip install torch==2.7.1+cu118 torchvision==0.22.1+cu118 \
#         --extra-index-url https://download.pytorch.org/whl/cu118
#   CPU only:
#     pip install torch==2.7.1 torchvision==0.22.1
#
# The OSNet ReID weights (osnet_ain_x1_0_msmt17.pt) download automatically on
# first run via gdown.
# ------------------------------------------------------------
torch==2.7.1
torchvision==0.22.1
ultralytics==8.4.66          # YOLO person detector
boxmot==20.0.0               # BoT-SORT tracker + OSNet ReID
supervision==0.28.0          # video IO, Detections, drawing
opencv-python==4.13.0.92
numpy==2.2.6
onemetric==0.1.2             # box_iou_batch
gdown==5.2.2                 # auto-download of ReID weights
lap==0.5.13                  # tracker data association
scipy==1.15.3
filterpy==1.4.5
